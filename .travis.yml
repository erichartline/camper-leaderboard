language: node_js
node_js:
  - "stable"
cache:
  directories:
    - node_modules
script:
  - npm test
  - npm run build
deploy:
  provider: surge
  local_dir: build
  domain: fcc-camper-leaderboard.surge.sh
  skip_cleanup: true
  on:
    branch: master
